name: 5mla

conda_env: conda.yaml

entry_points:
  main:
    parameters:
      train_path: path
      model_param1: {type: float, default: 0.5}
    command: "python train.py {train_path} {model_param1}"